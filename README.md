# RAG Assignment
Assignment 4 for Neural Networks class - North Auto part

Before running the backend:
cd backend/services
- python buildvectorstore.py 
(populates car info)
- python run_build_dealer.py
(populates dealership info)

To run backend:
cd backend
python app.py

To run frondend:
cd frontend
npm run dev